import React from 'react';
import PropTypes from 'prop-types';

import styles from './Slider.module.scss';

function Slider(props) {
  return <div className={styles.contentContainer}>Hello, Slider</div>;
}

Slider.propTypes = {};

export default Slider;
